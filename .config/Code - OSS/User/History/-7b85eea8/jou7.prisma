generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Agenda {
  id         String @id @default(uuid())
  title      String
  date       String
  time_start String
  time_end   String
  place      String
}
